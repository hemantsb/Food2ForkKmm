pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "KMPfood2fork"
include(":androidfood2forkApp")
include(":shared")